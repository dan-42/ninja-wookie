sudo: required
dist: trusty

language: cpp
compiler: g++

addons:
apt:
    sources: ['ubuntu-toolchain-r-test']
    packages: ['g++-4.9', 'cmake']

    

script:
  - mkdir build
  - cd ./build
  - cmake ..
  - make
  - ctest
