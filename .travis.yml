sudo: required
dist: trusty

language: cpp
compiler: g++

addons:
apt:
    sources: ['ubuntu-toolchain-r-test']
    packages: ['g++-5.3', 'cmake']

script:
  - mkdir build
  - cd ./build
  - cmake ..
  - make
  - ctest